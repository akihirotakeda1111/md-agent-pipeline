# Workspaces

The runner creates isolated temporary Git repositories for every case. No mutable
workspace fixture is committed here; this directory documents the same boundary used
by the Phase 3-5 suites.
